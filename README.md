# privacy-based-rag-system
Privacy-focused offline AI chatbot that runs entirely on-device with local language model inference. Ensures 100% data confidentiality with no cloud dependency, optimized for low-connectivity environments with reduced memory usage and faster response times
